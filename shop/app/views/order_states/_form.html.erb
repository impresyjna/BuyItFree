<%= form_for(@order_state) do |f| %>
  <% if @order_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_state.errors.count, "error") %> prohibited this order_state from being saved:</h2>

      <ul>
      <% @order_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nazwa", class: 'required' %><br>
    <%= f.text_field :name, :required => true %>
  </div>
  <div class="actions">
    <%= f.submit "Utwórz stan zamówienia", :class => "btn btn-success" %>
  </div>
<% end %>

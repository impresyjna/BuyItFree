<% provide(:title, 'Otrzymane wiadomosci') %>
<h1>Otrzymane wiadomosci</h1>

<%= will_paginate %>

<ul class="contacts">
    <%= render @contacts_with_respond %>
</ul>

<ul class="contacts">
    <%= render @contacts_without_respond%>
</ul>

<%= will_paginate %>